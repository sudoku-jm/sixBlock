const initialState = {
  dayBlockChecked : [],
}

export const DAY_BLOCK_CHECK = "DAY_BLOCK_CHECK"

const reducer = (state = initialState, action) => {
  switch (action.type){
    case DAY_BLOCK_CHECK:
      
  }
}